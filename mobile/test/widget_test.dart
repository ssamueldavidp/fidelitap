import 'package:flutter_test/flutter_test.dart';

// Widget tests require a real Supabase session and FCM credentials.
// Run integration tests on a connected device instead.
void main() {
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
